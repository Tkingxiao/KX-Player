# Dependencies
node_modules/

# Build outputs
dist/
dist-electron/
dist_electron/

# Electron build artifacts
*.exe
*.dmg
*.AppImage
*.deb
*.rpm
*.blockmap
# 注意：*.yml 会吞掉所有 .yaml/.yml 配置文件（含未来的 CI workflow），
# 因此显式放行 .github 下的工作流。negation 必须写在 *.yml 之后才生效。
*.yml
!.github/**/*.yml
!.github/**/*.yaml

# IDE
.reasonix/
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Claude Code
.claude/

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.local

#vscode 2022 build cache
.vs
CLAUDE.md
AGENTS.md

# Error
error.md
plan.md
better.md
record-time.js

# Rust / Tauri build artifacts
KX-Player/src-tauri/target/
**/target/
# libmpv-2.dll 由 build.rs 从 .toolchain/mpv-dev 铺过来，不入库（115 MB > GitHub 100 MB 单文件上限）
KX-Player/src-tauri/lib/
# tauri_build::build() 每次构建重新生成；capabilities/default.json 才是手写的事实源
KX-Player/src-tauri/gen/
# 机器专属：链接 mpv 的 native 搜索路径含绝对路径，模板见同目录 config.example.toml
KX-Player/src-tauri/.cargo/config.toml

# AI/agent tool state (临时钩子状态，勿入库)
.mimosa/
**/.mimosa/
.zcode/
**/.zcode/
.toolchain/
**/.toolchain/

# 本地测试媒体（开发期临时生成）
test-media/
**/test-media/

# 测试运行目录（临时 APPDATA / 日志 / dump）
.test-run/
**/.test-run/

# 运行期产物
kx-player-log.txt

# 构建 / 测试缓存（当前无被跟踪文件命中，可安全忽略）
*.tsbuildinfo
.vite/
coverage/
.eslintcache

# 编辑器备份 / 崩溃残留
*~
*.bak
*.tmp
*.old
*.stackdump
*.dmp

# 本地/临时文档
/docs-vibecoding
/tmp

# 逐批整改账本与交接笔记：含本机路径与逐批决策往来，只在本机保留
ALIGNMENT-AUDIT-*.md
HANDOVER*.md

# 本地启动脚本：写死了本机绝对路径兜底，不入库（等价入口是 npm run dev）
start.bat
# 根目录的 npm 空锁文件残留，真正生效的是 KX-Player/package-lock.json
/package-lock.json
