//! 改名主干（`04 §7`）：判定要不要动 → 本地规则能修的先修 → 剩下的才留给模型。
//!
//! 本模块**只读**：`norm`/`judge` 是纯函数，`preview` 只多了一处列目录，没有一处写盘。
//! 执行与回滚是应用层的事（`04 §7.8`），这样拆分的理由是判定的正确性只能靠用例证明，
//! 而一次写错的批量改名没有第二次机会。

pub mod judge;
pub mod listing;
pub mod norm;
pub mod preview;
