/**
 * 悬浮窗（pip）前后端共用常量。
 *
 * PIP_GUTTER：窗口内边到画面区的留白（CSS 逻辑像素）。画面从窗口下方透出，
 * 窗口尺寸 = 画面尺寸 + 2×PIP_GUTTER。取 0 即「无边框」：客户区就是画面本身，
 * 没有投影/描边那一圈，窗口边缘之外不再有可看出的边界。
 * PipRoot 的 CSS 与主窗口开窗尺寸都引用它，改一处即可。
 */
export const PIP_GUTTER = 0
